@page
@model TabApp.Pages_PersonWorker.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="CreatePerson">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.PersonWorker[0].Person.Address)</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PersonWorker) {
        <tr>
             <td>
                @Html.DisplayFor(modelItem => item.Person.Address)
            </td>
            <td>                
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
